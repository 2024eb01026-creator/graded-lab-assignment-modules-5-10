This script moves all files from a given directory into a backup directory.
Each move operation is performed in the background.
The script displays the PID of each background process and waits for all moves to complete.
