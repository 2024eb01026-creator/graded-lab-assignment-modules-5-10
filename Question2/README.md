This script accepts a log file as a command-line argument.
It validates the file and counts INFO, WARNING, and ERROR entries.
The script displays the most recent ERROR message.
A summary report file is generated with the current date.
Errors are handled with meaningful messages.
