This script compares two directories dirA and dirB.
It lists files unique to each directory.
For files present in both directories, it compares their contents using cmp.
No files are copied or modified.

